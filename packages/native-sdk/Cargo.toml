[package]
name = "primio-plugin-sdk"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Build-time Rust extension API for Primio"
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

