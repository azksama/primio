{
  "name": "primio",
  "private": true,
  "version": "0.2.10",
  "scripts": {
    "dev": "npm --prefix apps/client run dev",
    "build": "npm --prefix packages/sdk run build && npm --prefix packages/intro-skipper run build && npm --prefix apps/client run build",
    "test": "npm --prefix packages/sdk test && npm --prefix apps/client test"
  }
}
